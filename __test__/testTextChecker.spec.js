import { checkForText } from "../src/client/js/textChecker"

describe('Testing the text checker functionality', () => {
    test('Testing the checkForText() function', () => {
        expect(checkForText).toBeDefined();
    })
})
